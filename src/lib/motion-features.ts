// Набор возможностей framer-motion, загружаемый отдельным файлом.
//
// Смысл в разделении: сами компоненты `m.*` весят мало, а тяжёлый движок
// (анимации, layout-переходы) приезжает динамическим импортом уже после
// первого экрана. Для Mini App это важно — трафик мобильный.
//
// Нужен именно domMax, а не domAnimation: layoutId у переключателя
// «Расход/Доход» и layout у строк списка — это layout-анимации, которых в
// облегчённом наборе нет.
import { domMax } from 'framer-motion'

export default domMax
